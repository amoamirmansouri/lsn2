from setuptools import setup, find_packages

setup(name="fixer",
      version="0.2",
      license="MIT",
      packages=["fixer"],
      url="git@github.com:amoamirmansouri/lsn2.git",
      requires=["numpy"]
      )